{
  "routes": [
    {
      "method": "GET",
      "path": "/staff-profiles",
      "handler": "staff-profile.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/staff-profiles/count",
      "handler": "staff-profile.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/staff-profiles/:id",
      "handler": "staff-profile.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/staff-profiles",
      "handler": "staff-profile.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/staff-profiles/:id",
      "handler": "staff-profile.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/staff-profiles/:id",
      "handler": "staff-profile.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
